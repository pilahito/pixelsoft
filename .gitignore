# Dependencias y compilacion
node_modules/
.build/
apk/build/
apk/assets/

# Ejecutables generados (van en las Releases, no en el repositorio)
PixelSoft.exe
PixelSoft.apk
*.exe
*.apk
*.idsig

# Claves de firma: NUNCA al repositorio
*.keystore
*.jks

# Salidas sueltas de depuracion
_salida.txt
_error.txt
*.log

# Sistema
Thumbs.db
Desktop.ini
.DS_Store

# Motor de IA para el movil: ~41 MB de binarios. Se bajan con
# 'node tools/traer-ia.js' (el script del APK lo llama solo si faltan).
public/ia/

# Carpeta danada por el sistema de ficheros (pendiente de chkdsk). No es
# parte del proyecto: se quedo a medias al intentar borrarla.
# (tools-roto ya no existe: el proyecto se mudo a G: por corrupcion de E:)
